class FlavorOptions {
  final String entryModule

  FlavorOptions(String entryModule) {
    this.entryModule = entryModule
  }
}

rootProject.ext.flavors = [
        full: new FlavorOptions(":flavor.full"),
        slim: new FlavorOptions(":flavor.slim")
]

